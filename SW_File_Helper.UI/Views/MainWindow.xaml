<Window x:Class="SW_File_Helper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SW_File_Helper"
        xmlns:vm="clr-namespace:SW_File_Helper.ViewModels.Views"
        xmlns:controls="clr-namespace:SW_File_Helper.Controls"
        mc:Ignorable="d"      
        Height="450"
        Width="900"
        Title="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="0,0,0,7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <controls:EditableListView
                Title="Please Specify Source"
                    Margin="5" Grid.Column="0"/>

            <GridSplitter Grid.Column="1"
                          Style="{StaticResource GridSplitter_Vertical}"/>
            <controls:EditableListView
                    Margin="5" Grid.Column="2"
                Title="Please Specify Destination"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,7,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>

        <Grid Grid.Row="3" 
              x:Name="ButtonsGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>
